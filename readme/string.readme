字符串的内容可以用类似于数组下标的方式获取,但与数组不同,字符串的内容不能在初始
化后被修改,比如以下的例子:
str := "Hello world" // 字符串也支持声明时进行初始化的做法
str[0] = 'X'
// 编译错误

字符串遍历: 
	1. 同c 语言类似,数组形式遍历
	for i = 0; i < str_len; i++ {
		ch1 := str1[i]
		fmt.Println(i, ch1)
	}

Go  语言range方法遍历




